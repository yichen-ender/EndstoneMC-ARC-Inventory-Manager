# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
.Python
build/
dist/*.whl
dist/*.tar.gz
*.egg-info/
.eggs/
.venv/
venv/
.idea/
.vscode/
.DS_Store

# 本地会话临时文件（构建/编译输出、调试记录）—— 只匹配仓库根目录，
# 用前导斜杠锚定，否则 `_*.py` 会把包里的 `__init__.py` 一起忽略掉。
/_*.txt
/_*.py
*.old-1.2.1

# 本地重建的历史产物（不纳入版本控制）
dist-restored/
